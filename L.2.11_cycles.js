
while (true) {

    let num = prompt('Введите число', 0);

    if (num > 100 || num === null || num === '') break;
    
} 

/*
let num;

do {
    num = prompt('Введите число больше 100', 0);
} while (num <= 100 && num); 
Пока значение условия true цикл будет выполняться снова и снова*/ 